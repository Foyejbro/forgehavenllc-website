{
  "name": "forge-haven-store",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "python3 -m http.server 3000 -d public",
    "test": "node --test tests/*.test.mjs",
    "dev:api": "npx wrangler dev --config worker/wrangler.toml",
    "deploy:api": "node seed.mjs && npx wrangler d1 execute digital-store --remote --config worker/wrangler.toml --file worker/schema.sql && npx wrangler d1 execute digital-store --remote --config worker/wrangler.toml --file worker/seed.sql && npx wrangler deploy --config worker/wrangler.toml",
    "deploy": "npm run build && npm run deploy:api",
    "preview:worker": "npm run build && npx wrangler dev --config worker/wrangler.toml"
  }
}
