import policyText from './policy.mjs';
const POLICY='2026-09-22-v1', now=()=>new Date().toISOString(), enc=new TextEncoder();
const json=(v,status=200)=>new Response(JSON.stringify(v),{status,headers:{'Content-Type':'application/json'}});
const fail=(m,status=400)=>{throw Object.assign(new Error(m),{status})};
const hash=async s=>Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',enc.encode(s)))).map(x=>x.toString(16).padStart(2,'0')).join('');
const random=()=>Array.from(crypto.getRandomValues(new Uint8Array(32))).map(x=>x.toString(16).padStart(2,'0')).join('');
export function equal(a,b){if(typeof a!=='string'||typeof b!=='string'||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
export async function verifyStripe(raw,signature,secret,clock=Date.now()){if(!secret)return false;const parts=(signature||'').split(',').map(s=>s.split('='));const t=parts.find(p=>p[0]==='t')?.[1];if(!/^\d+$/.test(t||'')||Math.abs(clock/1000-Number(t))>300)return false;const key=await crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const bytes=new Uint8Array(await crypto.subtle.sign('HMAC',key,enc.encode(t+'.'+raw)));const h=Array.from(bytes).map(b=>b.toString(16).padStart(2,'0')).join('');return parts.some(p=>p[0]==='v1'&&equal(p[1],h))}
async function body(r){const t=await r.text();if(t.length>32000)fail('Request too large',413);try{return JSON.parse(t)}catch{fail('Invalid JSON')}}
const run=(env,sql,...args)=>env.DB.prepare(sql).bind(...args).run();
const one=(env,sql,...args)=>env.DB.prepare(sql).bind(...args).first();
const all=async(env,sql,...args)=>(await env.DB.prepare(sql).bind(...args).all()).results;
async function event(env,id,type,detail={}){await run(env,'INSERT INTO events(order_id,type,detail,created_at) VALUES(?,?,?,?)',id,type,JSON.stringify(detail),now())}
async function catalog(env){return(await all(env,'SELECT data FROM products')).map(p=>JSON.parse(p.data))}
async function orderAuth(r,env,id){const o=await one(env,'SELECT * FROM orders WHERE id=?',id);const token=(r.headers.get('Authorization')||'').replace(/^Bearer /,'');if(!o||!equal(o.token_hash,await hash(token)))fail('Order or access key is incorrect',403);return o}
async function limit(r,env){const ip=r.headers.get('CF-Connecting-IP')||'local',slot=Math.floor(Date.now()/3600000),key=await hash(ip+slot);await run(env,'DELETE FROM rate_limits WHERE expires < ?',Date.now());const hit=await env.DB.prepare('INSERT INTO rate_limits(key,count,expires) VALUES(?,1,?) ON CONFLICT(key) DO UPDATE SET count=count+1 RETURNING count').bind(key,Date.now()+3600000).first();if(hit.count>30)fail('Too many orders. Please try again later.',429)}
function safeLink(s){if(!s)return '';try{const u=new URL(s);if(u.protocol==='https:')return u.href}catch{}fail('A valid HTTPS URL is required')}
function text(s,max=2000){return String(s||'').trim().slice(0,max)}
export function validateProduct(p){if(!/^[a-z0-9-]{1,80}$/.test(p.id))fail('Use a lowercase product ID with hyphens');if(!['Logo','Photograph','Graphic Design','Stock','PSD'].includes(p.category))fail('Invalid category');if(!Number.isSafeInteger(p.price)||p.price<50||p.price>10000000)fail('Invalid price (USD cents)');if(!['custom','instant'].includes(p.delivery)||!p.title||!p.description||!p.license)fail('Complete all product details');if(p.category==='Logo'&&p.delivery!=='custom')fail('Logo orders must be custom');if(p.active&&(!p.preview||(p.delivery==='instant'&&!p.file_key)))fail('Active products require a preview and instant products require a private file');if(p.preview)safeLink(p.preview);return p}
async function route(r,env){const u=new URL(r.url),path=u.pathname,method=r.method;
if(path==='/api/catalog'&&method==='GET'){const products=(await catalog(env)).map(({file_key,...p})=>p);const b=await one(env,"SELECT value FROM settings WHERE key='banner'");return json({products,banner:b?JSON.parse(b.value):null})}
if(path==='/api/reviews'&&method==='GET')return json(await all(env,'SELECT product_id,product_title,rating,display_name,text,created_at FROM reviews WHERE approved=1 ORDER BY created_at DESC LIMIT 60'));
if(path==='/api/stripe/webhook'&&method==='POST'){const raw=await r.text();if(!await verifyStripe(raw,r.headers.get('stripe-signature'),env.STRIPE_WEBHOOK_SECRET))fail('Invalid webhook signature',400);const e=JSON.parse(raw),s=e.data?.object;if(e.type==='checkout.session.completed'||e.type==='checkout.session.async_payment_succeeded'){if(s.payment_status==='paid'){const o=await one(env,'SELECT * FROM orders WHERE id=?',s.metadata?.order_id||'');if(!o||o.method!=='stripe'||o.stripe_session!==s.id||s.amount_total!==o.total||s.currency!==o.currency)fail('Order mismatch',409);await run(env,"UPDATE orders SET status='paid',payment_reference=? WHERE id=? AND status='pending'",String(s.payment_intent||s.id),o.id);await event(env,o.id,'stripe_payment_confirmed',{event:e.id,session:s.id,total:s.amount_total})}}if(e.type==='charge.refunded'){const o=await one(env,'SELECT * FROM orders WHERE payment_reference=?',String(s.payment_intent||''));if(o){await run(env,"UPDATE orders SET status='refunded' WHERE id=?",o.id);await event(env,o.id,'stripe_refund',{event:e.id})}}if(e.type==='charge.dispute.created'){const o=await one(env,'SELECT * FROM orders WHERE payment_reference=?',String(s.payment_intent||''));if(o){await run(env,"UPDATE orders SET status='disputed' WHERE id=?",o.id);await event(env,o.id,'stripe_dispute',{event:e.id})}}return json({received:true})}
if(path==='/api/checkout'&&method==='POST'){const d=await body(r);if(d.accepted!==true||d.policyVersion!==POLICY)fail('Please accept the current policies');if(!['stripe','relay'].includes(d.method))fail('Invalid payment method');if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(d.email||'')||!text(d.name))fail('Valid name and email are required');if(!Array.isArray(d.items)||!d.items.length||d.items.length>20)fail('Invalid cart');if(new Set(d.items.map(x=>x.id)).size!==d.items.length)fail('Duplicate products');if(d.method==='stripe'&&!env.STRIPE_SECRET_KEY)fail('Card checkout is not configured yet',503);await limit(r,env);const ps=await catalog(env),items=[];for(const requested of d.items){const p=ps.find(x=>x.id===requested.id);if(!p||!p.active)fail('A product is unavailable');validateProduct(p);if(p.delivery==='custom'&&!text(requested.company,150))fail('Company name is required');if(p.delivery==='instant'&&!await env.FILES.head(p.file_key))fail('Product file is unavailable. Contact support.',409);items.push({...p,company:text(requested.company,150),tagline:text(requested.tagline,150),instructions:text(requested.instructions),ready:p.delivery==='instant',file_key:p.delivery==='instant'?p.file_key:''})}const total=items.reduce((s,p)=>s+p.price,0),id=crypto.randomUUID(),token=random();await run(env,'INSERT INTO orders(id,token_hash,name,email,method,total,items,policy_version,accepted_at,created_at) VALUES(?,?,?,?,?,?,?,?,?,?)',id,await hash(token),text(d.name,120),text(d.email,254),d.method,total,JSON.stringify(items),POLICY,now(),now());await event(env,id,'policy_accepted',{version:POLICY,policyText,items:items.map(p=>({id:p.id,license:p.license})),deliveryRequested:true});if(d.method==='relay')return json({id,token});const params=new URLSearchParams({mode:'payment',customer_email:text(d.email,254),'payment_method_types[0]':'card',success_url:env.STORE_ORIGIN+'/?order='+id,cancel_url:env.STORE_ORIGIN+'/?order='+id,'metadata[order_id]':id,'payment_intent_data[metadata][order_id]':id,client_reference_id:id});items.forEach((p,i)=>{params.set(`line_items[${i}][price_data][currency]`,'usd');params.set(`line_items[${i}][price_data][unit_amount]`,String(p.price));params.set(`line_items[${i}][price_data][product_data][name]`,p.title);params.set(`line_items[${i}][quantity]`,'1')});const res=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{Authorization:'Bearer '+env.STRIPE_SECRET_KEY,'Content-Type':'application/x-www-form-urlencoded','Idempotency-Key':id},body:params});const s=await res.json();if(!res.ok||!s.url){await run(env,"UPDATE orders SET status='failed' WHERE id=?",id);fail('Payment service unavailable. Please try again.',502)}await run(env,'UPDATE orders SET stripe_session=? WHERE id=?',s.id,id);return json({id,token,url:s.url})}
const match=path.match(/^\/api\/orders\/([a-f0-9-]+)(?:\/(.*))?$/);if(match){const o=await orderAuth(r,env,match[1]),action=match[2]||'',items=JSON.parse(o.items);if(!action&&method==='GET'){return json({id:o.id,status:o.status,method:o.method,total:o.total,items:items.map(({file_key,...p})=>p),relayUrl:o.relay_url||'',relayInstructions:env.RELAY_INSTRUCTIONS||'',paymentReference:o.payment_reference})}
if(action==='reference'&&method==='POST'){if(o.method!=='relay'||o.status!=='pending')fail('This order cannot accept a payment reference');const d=await body(r),reference=text(d.reference,200);if(!reference)fail('Reference is required');await run(env,'UPDATE orders SET payment_reference=? WHERE id=?',reference,o.id);await event(env,o.id,'customer_payment_reference',{reference});return json({ok:true})}
if(action==='review'&&method==='POST'){if(o.status!=='paid')fail('Only paid customers can review',403);const d=await body(r),p=items.find(p=>p.id===d.productId),rating=Number(d.rating);if(!p||!Number.isInteger(rating)||rating<1||rating>5||!text(d.displayName,60))fail('Invalid review');await run(env,'INSERT INTO reviews(order_id,product_id,product_title,rating,display_name,text,created_at) VALUES(?,?,?,?,?,?,?) ON CONFLICT(order_id,product_id) DO UPDATE SET rating=excluded.rating,display_name=excluded.display_name,text=excluded.text,approved=0',o.id,p.id,p.title,rating,text(d.displayName,60),text(d.text),now());return json({ok:true})}
if(action==='receipt'&&method==='GET'){if(o.status!=='paid')fail('License is issued after payment confirmation',403);return new Response(`FORGE HAVEN LLC — LICENSE RECEIPT\nOrder: ${o.id}\nLicensed to: ${o.name} (${o.email})\nPayment: ${o.method} / ${o.payment_reference||o.received_reference||''}\nTotal: USD ${(o.total/100).toFixed(2)}\nPolicy: ${o.policy_version}\nAccepted: ${o.accepted_at}\n\n${items.map(p=>p.title+'\n'+p.license).join('\n\n')}\n\nCopyright remains with the creator. support@forgehavenllc.org`,{headers:{'Content-Type':'text/plain; charset=utf-8','Content-Disposition':'attachment; filename="forge-haven-license.txt"'}})}
if(action.startsWith('download/')&&method==='GET'){if(o.status!=='paid')fail('Payment is not confirmed',403);const p=items.find(p=>p.id===action.slice(9));if(!p?.ready||!p.file_key)fail('File is not ready',404);const obj=await env.FILES.get(p.file_key);if(!obj)fail('File unavailable. Please contact support.',404);await event(env,o.id,'download_served',{productId:p.id,key:p.file_key});const h=new Headers();obj.writeHttpMetadata(h);h.set('Content-Disposition','attachment; filename="'+p.file_key.split('/').pop().replace(/[^a-zA-Z0-9._-]/g,'_')+'"');h.set('Content-Type','application/octet-stream');return new Response(obj.body,{headers:h})}fail('Not found',404)}
if(path.startsWith('/api/admin/')){const token=(r.headers.get('Authorization')||'').replace(/^Bearer /,'');if(!env.ADMIN_TOKEN||env.ADMIN_TOKEN.length<32||!equal(token,env.ADMIN_TOKEN))fail('Unauthorized',401);const action=path.slice(11);if(action==='data'&&method==='GET')return json({products:await catalog(env),orders:await all(env,'SELECT id,name,email,method,status,total,items,payment_reference,received_reference,relay_url,created_at FROM orders ORDER BY created_at DESC LIMIT 200'),reviews:await all(env,'SELECT * FROM reviews ORDER BY created_at DESC LIMIT 200')});if(action==='product'&&method==='POST'){const p=validateProduct(await body(r));if(p.active&&p.delivery==='instant'&&!await env.FILES.head(p.file_key))fail('Upload the file to R2 first');await run(env,'INSERT INTO products(id,data) VALUES(?,?) ON CONFLICT(id) DO UPDATE SET data=excluded.data',p.id,JSON.stringify(p));return json({ok:true})}if(action==='banner'&&method==='POST'){const b=await body(r);b.url=safeLink(b.url);if(b.image)b.image=safeLink(b.image);await run(env,"INSERT INTO settings(key,value) VALUES('banner',?) ON CONFLICT(key) DO UPDATE SET value=excluded.value",JSON.stringify(b));return json({ok:true})}if(action==='order'&&method==='POST'){const d=await body(r),o=await one(env,'SELECT * FROM orders WHERE id=?',d.id);if(!o)fail('Order not found',404);if(d.action==='relay-link'){if(o.method!=='relay'||o.status!=='pending')fail('Only pending Relay orders');const link=safeLink(d.url);await run(env,'UPDATE orders SET relay_url=? WHERE id=?',link,o.id);await event(env,o.id,'relay_request_added',{url:link})}else if(d.action==='confirm-relay'){if(o.method!=='relay'||o.status!=='pending'||!text(d.reference,200))fail('Verify received funds and enter transaction reference');await run(env,"UPDATE orders SET status='paid',received_reference=? WHERE id=? AND status='pending'",text(d.reference,200),o.id);await event(env,o.id,'admin_relay_verified',{reference:text(d.reference,200)})}else if(d.action==='deliver'){if(o.status!=='paid')fail('Payment must be confirmed first');const items=JSON.parse(o.items),p=items.find(x=>x.id===d.productId&&x.delivery==='custom');if(!p||!d.fileKey||!await env.FILES.head(d.fileKey))fail('Upload the custom file to R2 first');p.file_key=text(d.fileKey,500);p.ready=true;await run(env,'UPDATE orders SET items=? WHERE id=?',JSON.stringify(items),o.id);await event(env,o.id,'custom_delivered',{productId:p.id,key:p.file_key})}else if(d.action==='revoke'){await run(env,"UPDATE orders SET status='revoked' WHERE id=?",o.id);await event(env,o.id,'access_revoked',{reason:text(d.reason)})}else fail('Unknown order action');return json({ok:true})}if(action==='review'&&method==='POST'){const d=await body(r);await run(env,'UPDATE reviews SET approved=? WHERE order_id=? AND product_id=?',d.approved?1:0,d.orderId,d.productId);return json({ok:true})}if(action.startsWith('evidence/')&&method==='GET'){const id=action.slice(9),o=await one(env,'SELECT * FROM orders WHERE id=?',id);if(!o)fail('Not found',404);delete o.token_hash;return json({order:o,events:await all(env,'SELECT * FROM events WHERE order_id=? ORDER BY id',id)})}fail('Not found',404)}
fail('Not found',404)}
export default {async fetch(r,env){const path=new URL(r.url).pathname;
if(!path.startsWith('/api/')) return env.ASSETS.fetch(r);
const origin=r.headers.get('Origin');let response;try{if(origin&&origin!==env.STORE_ORIGIN)fail('Origin not allowed',403);if(r.method==='OPTIONS')response=new Response(null,{status:204});else response=await route(r,env)}catch(e){response=json({error:e.status?e.message:'Server error. Please contact support.'},e.status||500)}const h=new Headers(response.headers);h.set('Cache-Control','no-store');h.set('X-Content-Type-Options','nosniff');h.set('Referrer-Policy','no-referrer');if(origin===env.STORE_ORIGIN){h.set('Access-Control-Allow-Origin',origin);h.set('Vary','Origin');h.set('Access-Control-Allow-Headers','Content-Type, Authorization');h.set('Access-Control-Allow-Methods','GET,POST,OPTIONS');h.set('Access-Control-Expose-Headers','Content-Disposition')}return new Response(response.body,{status:response.status,headers:h})}};
