const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let token='',data={},tab='products';
async function api(path,body){if(!window.STORE_CONFIG.apiBase)throw Error('Set apiBase in config.js after deploying your Worker.');const r=await fetch(window.STORE_CONFIG.apiBase+'/api/admin/'+path,{method:body?'POST':'GET',headers:{Authorization:'Bearer '+token,'Content-Type':'application/json'},...(body?{body:JSON.stringify(body)}:{})});const d=await r.json();if(!r.ok)throw Error(d.error);return d}
function message(s){$('#message').textContent=s}async function refresh(){data=await api('data');render()}
$('#login').onsubmit=async e=>{e.preventDefault();token=$('#token').value;try{await refresh();$('#login').classList.add('hidden');$('#dashboard').classList.remove('hidden');$('#token').value='';message('Dashboard unlocked. Token stays in memory only.')}catch(e){message(e.message)}};
$('#logout').onclick=()=>location.reload();document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});
function exportFile(d,name){const url=URL.createObjectURL(new Blob([JSON.stringify(d,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}$('#export').onclick=()=>exportFile(data.products,'catalog.json');
function render(){const c=$('#content');if(tab==='products'){c.innerHTML='<h2>Products</h2><p class="note">Sample products are disabled. Upload private deliverables to R2, add a public watermarked preview and accurate details, then activate. After edits, export catalog.json, replace the project catalog and run npm run build for updated SEO pages.</p><button id="new">Add product</button>'+data.products.map(p=>`<div class="admin-row"><strong>${esc(p.title)}</strong> · ${esc(p.category)} · $${(p.price/100).toFixed(2)} · ${p.active?'Active':'Demo / inactive'}<button data-edit="${esc(p.id)}">Edit</button></div>`).join('');$('#new').onclick=()=>edit({id:'',title:'',category:'Logo',price:4900,delivery:'custom',description:'',license:'',format:'',preview:'',file_key:'',active:false,art:'stock',label:''});document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>edit(data.products.find(p=>p.id===b.dataset.edit)))}
if(tab==='orders'){c.innerHTML='<h2>Orders</h2><p>Relay: create an exact-amount request in Relay, attach the link here, then confirm only after funds are received. Confirmation unlocks instant downloads. Custom files unlock when delivered.</p>'+data.orders.map(o=>`<div class="admin-row"><strong>${esc(o.name)}</strong> · ${esc(o.email)}<p>${esc(o.id)}<br>${esc(o.method)} · ${esc(o.status)} · $${(o.total/100).toFixed(2)}<br>Submitted reference: ${esc(o.payment_reference||'None')}</p>${JSON.parse(o.items).map(p=>`<p>${esc(p.title)} — ${esc(p.company||'')} ${esc(p.tagline||'')}<br>${esc(p.instructions||'')}</p>${p.delivery==='custom'&&o.status==='paid'?`<button data-deliver="${esc(o.id)}" data-product="${esc(p.id)}">Deliver ${esc(p.title)}</button>`:''}`).join('')}${o.method==='relay'&&o.status==='pending'?`<button data-relay="${esc(o.id)}">Attach Relay payment link</button><button data-confirm="${esc(o.id)}">Confirm received funds</button>`:''}<button data-evidence="${esc(o.id)}">Export evidence</button><button data-revoke="${esc(o.id)}">Revoke downloads</button></div>`).join('');}
if(tab==='reviews'){c.innerHTML='<h2>Review moderation</h2><p>Only paid buyers can submit. Moderate spam and abuse consistently; do not remove legitimate negative feedback.</p>'+data.reviews.map((r,i)=>`<div class="admin-row"><strong>${esc(r.display_name)}</strong> · ${r.rating}/5 · ${esc(r.product_title)}<p>${esc(r.text||'Rating only')}</p><button data-review="${i}" data-approved="${r.approved?'0':'1'}">${r.approved?'Unpublish':'Publish'}</button></div>`).join('')}
if(tab==='banner'){c.innerHTML='<h2>Promotion banner</h2><p class="note">A discount banner does not change prices. Set actual sale prices in Products before advertising an offer. No offer is active by default.</p><form id="banner-form">'+['label','title','text','cta','url','image','big'].map(k=>`<label>${k}${k==='title'?' (use a line break for second line)':''}<${k==='title'?'textarea':'input'} name="${k}" ${['label','title','text','cta','url'].includes(k)?'required':''}>${k==='title'?'</textarea>':''}</label>`).join('')+'<button class="primary">Save banner</button></form>';$('#banner-form').onsubmit=async e=>{e.preventDefault();try{await api('banner',Object.fromEntries(new FormData(e.target)));message('Banner saved.')}catch(e){message(e.message)}}}}
function edit(p){$('#content').innerHTML='<h2>Edit product</h2><form id="product-form"><div class="form-grid">'+['id','title','price','format','preview','file_key','label'].map(k=>`<label>${k}${k==='price'?' (USD cents: 4900 = $49)':''}<input name="${k}" value="${esc(p[k])}" ${k==='price'?'type="number" min="50"':''} ${['id','title','price'].includes(k)?'required':''} ${k==='id'&&p.id?'readonly':''}></label>`).join('')+`<label>Category<select name="category">${['Logo','Photograph','Graphic Design','Stock','PSD'].map(x=>`<option ${x===p.category?'selected':''}>${x}</option>`).join('')}</select></label><label>Delivery<select name="delivery"><option value="custom" ${p.delivery==='custom'?'selected':''}>Custom</option><option value="instant" ${p.delivery==='instant'?'selected':''}>Instant</option></select></label></div><label>Description<textarea name="description" required>${esc(p.description)}</textarea></label><label>License<textarea name="license" required>${esc(p.license)}</textarea></label><label class="check"><input name="active" type="checkbox" ${p.active?'checked':''}>Active and ready to sell</label><button class="primary">Save product</button></form>`;$('#product-form').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));try{await api('product',{...p,...d,price:Number(d.price),active:d.active==='on'});await refresh();message('Saved. Export catalog and rebuild for SEO pages.')}catch(e){message(e.message)}}}
$('#content').addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;try{if(b.dataset.relay){const url=prompt('Paste the HTTPS Relay payment request link for this exact order');if(url)await api('order',{id:b.dataset.relay,action:'relay-link',url})}else if(b.dataset.confirm){const reference=prompt('Verify money received in Relay first. Enter the bank transaction ID:');if(reference&&confirm('Have you checked the received amount and currency against this order?'))await api('order',{id:b.dataset.confirm,action:'confirm-relay',reference})}else if(b.dataset.deliver){const fileKey=prompt('Exact R2 key of this customer’s completed file:');if(fileKey)await api('order',{id:b.dataset.deliver,action:'deliver',productId:b.dataset.product,fileKey})}else if(b.dataset.revoke){const reason=prompt('Reason for revoking downloads (does not issue a refund):');if(reason)await api('order',{id:b.dataset.revoke,action:'revoke',reason})}else if(b.dataset.evidence){exportFile(await api('evidence/'+b.dataset.evidence),'order-evidence.json');return}else if(b.dataset.review){const r=data.reviews[+b.dataset.review];await api('review',{orderId:r.order_id,productId:r.product_id,approved:b.dataset.approved==='1'})}else return;await refresh();message('Updated.')}catch(e){message(e.message)}});
